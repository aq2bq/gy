# n-8c5e (N-2) フィットの計算にラベルの広がりを含め、端でラベルが切れないようにする

state: closed
scope: html_projection
created: 2026-09-13
  targets ac-670c (AC-3) 表示対象が変わったときに自動でフィットし、その直後のノードラベルの文字高が11ピクセル以上である


閉じた理由: 事実（未実装のまま閉じた。HTML 投影は D-79 で消す (D-80)）

