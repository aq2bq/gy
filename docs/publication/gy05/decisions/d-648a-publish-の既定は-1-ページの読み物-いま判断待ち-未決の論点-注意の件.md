# d-648a (D-87) publish の既定は 1 ページの読み物（いま判断待ち、未決の論点、注意の件数、期間の変更）とし、ノードの記述（問い 1・3・4）は指定した ID の分だけ出す。全ノードの書き出しはしない

- 種類: decision
- scope: gy05
- created: 2026-09-15
- 別名: D-87

## 関係

- narrowed-by d-38b5 publish <ID> の答えは「見出しと箇条書き」の形にする: 見出しは旧 ID と短い題、節は 決めたこと / 理由 / 生んだ作業（状態付き）/ 状態（有効か、置き換えの有無）。要求は 来歴 / 状態 / 記録、ニーズは 目的 / 受け入れ条件 / 起票した要求（mark: 問い 1・3・4 は publish <ID>... で指定したノードだけ）
- narrows d-799e (D-85) publish の読み物は 6 つの問いに答える形で作り、マスターが読んで答えられなかった問いを後から足す（mark: 6 つの問いに答える形で作り）
- spawns n-b6f3 (N-72) publish を 1 ページの読み物に直す: 既定はいま判断待ち・未決・注意の件数・期間の変更だけ、ノードの記述は publish <ID>... で指定した分だけ
- superseded-by d-edb0 publish は開発の成果物としてコミットし後から過去の判断と経緯を振り返るための記録であり、開発中にマスターが読むものではない。「D-78 って何だっけ」に答えるのは WebUI のニーズで、gy のコマンドには人間向けの出力の形を足さない（mark: 問い 2・5・6 は既定の読み物）

## 成立範囲

2026-09-15 マスターの指摘「人質でも取られない限り、人間は 1758 行の怪文書を読むことはない」を受けた lead の設計の訂正。N-71 で lead が依頼した「ノードの節に全ノードを並べる」が誤りだった（前回の HTML 投影と同じ失敗の型: 機能を並べて読み手を失う）。D-85 の 6 つの問いの答え方を分ける: 問い 2・5・6 は既定の読み物（1 画面、目安 60 行以内。注意は種類ごとの件数だけ）、問い 1・3・4 は publish <ID>... で指定したノードだけ（エージェントがマスターの「D-78 って何だっけ」に応じて出す）。--out の既定は stdout。AC-52 の測定はこの形で行う。

## 本文


## Context

## Decision

## Consequences

## 自由属性

- 無し

