# d-7c64 publish の出力は 1 ファイルでなく、スコープごとのディレクトリに 1 ノード 1 ファイルと索引 1 ファイルを書き出す形にし、出力先のスコープのディレクトリの中だけを作り直す

- 種類: decision
- scope: gy05
- created: 2026-09-15

## 関係

- narrows d-edb0 publish は開発の成果物としてコミットし後から過去の判断と経緯を振り返るための記録であり、開発中にマスターが読むものではない。「D-78 って何だっけ」に答えるのは WebUI のニーズで、gy のコマンドには人間向けの出力の形を足さない（mark: 指定した時点と範囲の記録（ノード・辺・履歴）と診断結果を生成する公開物）
- spawns n-1aad edit --set scope=<名前> でノードを別のスコープへ移せるようにする（名前は gy.toml にあるものだけ。ID と辺は変わらない）
- spawns n-45ef publish (a): スコープごとのディレクトリ出力と 1 ノード 1 ファイル（関係と mark を相手の題名付きで。対象スコープのディレクトリだけを作り直す）
- spawns n-5c59 publish (b): スコープごとの索引 README.md（見出し・読み方・一覧とリンク・履歴・診断）と --since、gy.toml の output、文書、gy 自身の docs/publication の作り直し（n-45ef から分割）
- spawns n-ef16 publish のノードのファイルの形を整える: 関係は 1 か所（両向き）、成立範囲は 1 回、節の間に空行、書き手が無ければ行を出さない

## 成立範囲

2026-09-15 Kokopelli の進行管理担当の依頼（マスターの決定: 決定の正本は gy、人とエージェントが読む公開物は publish で docs/gy-published/<スコープ>/ へ書き出してコミットする。docs/adr の取り込み済み 0001〜0177 は消す）。d-edb0（publish は振り返りの記録、完結した文書）を狭める: 完結性は各ファイルが関係を相手の題名付きで持ち、索引が読み方を持つことで保つ。出力先は --out（gy.toml の output）で、ディレクトリとして扱う。ファイル名は <ID>-<題の安全な形>.md。索引は <スコープ>/README.md（読み方、種類ごとの一覧、履歴、診断）。publish は出力先の外に触らず、対象スコープのディレクトリを消して作り直す。対象は Kokopelli と gy 自身の 2 台帳、全ノード。

## 本文

## 自由属性

- 無し

