# d-edb0 publish は開発の成果物としてコミットし後から過去の判断と経緯を振り返るための記録であり、開発中にマスターが読むものではない。「D-78 って何だっけ」に答えるのは WebUI のニーズで、gy のコマンドには人間向けの出力の形を足さない

## 関係
- supersedes d-648a (D-87) publish の既定は 1 ページの読み物（いま判断待ち、未決の論点、注意の件数、期間の変更）とし、ノードの記述（問い 1・3・4）は指定した ID の分だけ出す。全ノードの書き出しはしない（mark: 問い 2・5・6 は既定の読み物）
- narrowed-by d-7c64 publish の出力は 1 ファイルでなく、スコープごとのディレクトリに 1 ノード 1 ファイルと索引 1 ファイルを書き出す形にし、出力先のスコープのディレクトリの中だけを作り直す（mark: 指定した時点と範囲の記録（ノード・辺・履歴）と診断結果を生成する公開物）

decision_scope: 2026-09-15 マスターの指摘。lead は D-85 と D-87 で publish を「マスターが読む読み物」と誤って解釈し、1 ページ化（N-72）と見出しと箇条書き（n-6eae）を進めた。正しくは D-61 のとおり、publish は指定した時点と範囲の記録（ノード・辺・履歴）と診断結果を生成する公開物で、リポジトリにコミットして振り返りに使う。使うのはエージェント。gy のコマンドは AI 向けで、人間向けのインターフェースは足さない。D-85 の 6 つの問いは WebUI のニーズ（第 3 段）の入力に移す。
scope: gy05
created: 2026-09-15
supplement: 2026-09-15 マスター「公開成果物を理解するために原本を読む必要があってはいけない。完結した文書である必要がある」。publish の出力は、ノードの参照をすべて ID + 題名で書き、関係・状態・種類の意味を文書内の凡例で説明し、ノードの全項目を逐語で含む
  supersedes d-648a (D-87) publish の既定は 1 ページの読み物（いま判断待ち、未決の論点、注意の件数、期間の変更）とし、ノードの記述（問い 1・3・4）は指定した ID の分だけ出す。全ノードの書き出しはしない（mark: 問い 2・5・6 は既定の読み物）
  narrowed-by d-7c64 publish の出力は 1 ファイルでなく、スコープごとのディレクトリに 1 ノード 1 ファイルと索引 1 ファイルを書き出す形にし、出力先のスコープのディレクトリの中だけを作り直す（mark: 指定した時点と範囲の記録（ノード・辺・履歴）と診断結果を生成する公開物）
  spawns n-02b9 WebUI（第 3 段）: マスターが「これは何で何と関係があるか」「今マスター待ちは何か」を把握できる画面。問いは D-85 の 6 つを入力にし、作るのはマスターの指示があってから
  spawns n-8f3f publish を記録の公開物にする: 指定した時点と範囲のノード・辺・履歴と診断結果を 1 ファイルに出し、--out（gy.toml の output）へ書いてコミットできる形にする。1 ページ化（N-72）は戻す

