---
type: spec
title: lak — 決定・論点・ニーズを1つのグラフとして扱う進行管理CLI
status: draft
version: 0.1
date: 2026-09-11
---

# lak 発注文書

## 0. この文書の読み方

本書は `lak` の外形的要求を定める。実装方針・内部設計は受注側の裁量とする。

**「必須」と書かれたものは満たすこと。「推奨」はドッグフーディングで変わりうる。「未定」は実装せず、拡張できる形だけ残すこと。**

最初のリリースで全コマンドを完成させる。段階リリースはしない。利用者は発注者本人とAIエージェントであり、完成直後からドッグフーディングに入る。

---

## 1. 目的

### 1.1 解決する問題

複数のAIエージェント（進行管理 / 要求定義 / 設計 / 実装 / 調査）のオーケストレーションで、大規模な基幹システムの機能開発を並行して進めている。プロジェクト終盤になるほど進行管理が破綻する。

破綻は**決定の内部では起きていない**。決定と論点とニーズの**あいだ**で起きている。実際に観測された失敗：

- 論点が閉じたのに、別の行が同じ論点を「決着待ち」として参照したまま残った
- ニーズが受け入れ条件の対応表から抜け落ち、進捗の俯瞰から消えた
- 後続が引く制約が、決定として立たないまま要求の記録が圧縮され、失われた
- 新しい決定が古い決定の成立範囲を狭めたが、古い決定の本文だけを読んだ担当が誤った
- 手で書いた台帳の表で、決定文が決定者の列に入り込んだ
- 完了した要求の行が積み上がり、GitHub Issue の本文上限に達した

既存の ADR ツール（[adrs](https://github.com/joshrotenberg/adrs)）は決定だけを扱う。決定は1ファイルに閉じるため、ファイル操作で足りる。**あいだは足りない。**

### 1.2 このツールの主張

**あいだをグラフに載せれば、規律は検査になる。検査になったものは文書から消える。**

現在この規律は約1300行の運用文書（エージェントハーネス）が担っている。`lak` の完成により、そのうち9項目が文書から消え、300行になる。残るのは機械が判定できないものだけになる。

### 1.3 非目標

- 汎用のプロジェクト管理ツールにしない。GitHub Issue / PR を置き換えない
- 優先順位を付けない。並べるだけで、選ぶのは人間である
- 台帳とコードの実態の乖離は検査しない（グラフの内部整合のみを見る）
- Web UI を作らない。CLI と生成された Markdown のみ

---

## 2. 利用者と実行環境

| 主体 | 使い方 |
|---|---|
| AIエージェント | 全コマンドを実行する。主たる利用者。標準出力を読んで次の行動を決める |
| 人間（発注者） | `render` の出力を読む。`find` / `show` / `next` / `stats` を対話的に使う |
| CI / pre-commit | `lint` を終了コードで判定する |

エージェントが主たる利用者である点が設計を規定する。**エラーメッセージと `--help` は、消えた運用文書の代替として機能しなければならない**（§9）。

---

## 3. 技術前提

| 項目 | 指定 |
|---|---|
| 言語 | Rust（必須） |
| 参考実装 | [joshrotenberg/adrs](https://github.com/joshrotenberg/adrs)。構成・CLI設計・配布方法を踏襲してよい |
| 構成 | ワークスペース。`lak-core`（ライブラリ）+ `lak`（CLI）。adrs の `adrs-core` / `adrs` と同じ分離 |
| CLI | clap（derive）。サブコマンド構成 |
| 配布 | `cargo install lak` を必須。Homebrew / バイナリリリースは推奨 |
| プラットフォーム | macOS / Linux 必須。Windows 推奨 |
| シェル補完 | `lak completions <shell>` 必須 |

### 3.1 adrs から踏襲するもの

以下は adrs に実装があり、`lak` でも同じ役割を果たす。実装を参考にしてよい。

| adrs | lak | 備考 |
|---|---|---|
| `adrs-core` / `adrs` の分離 | `lak-core` / `lak` | 将来の MCP サーバ・他ツール連携のため |
| `.adr-dir` による設定探索（サブディレクトリからでも台帳を発見） | `.lak-dir` 相当 | サブディレクトリからの実行を必須とする |
| `--ng` の YAML frontmatter モード | 常時 frontmatter | lak は frontmatter が正本。モード切替はない |
| `doctor` + `adrs.toml` の `[doctor]` で検査ルールを設定 | `lint` + `lak.toml` の `[lint]` | 検査項目の個別 on/off・重大度設定に使う |
| `.pre-commit-hooks.yaml` | 同じ | `lak lint` を pre-commit で回す |
| `link` が逆向きリンクを自動導出 | 同じ | §6.5 |
| `generate graph`（Graphviz 出力） | `show --graph` / `render --format dot` | |
| MCP サーバ内蔵 | §10 | エージェントが主利用者なので重要度が高い |
| `cheatsheet` | 同じ | エージェントの初回セッションで読ませる |

### 3.2 adrs から踏襲しないもの

- ADR のフォーマット種別（Nygard / MADR）とテンプレート variant の切り替え。`lak` のノードは lak が定める1形式のみ
- 番号の振り直し（`import --renumber`）。ID は不変である（§4.3）

---

## 4. データモデル

### 4.1 ノード

| 種別 | ID書式 | 意味 |
|---|---|---|
| need | `N-<n>` | まだ要求になっていないもの |
| question | `Q-<n>` | 未決の論点 |
| decision | `D-<n>` | 決定。ADR に相当する |
| requirement | `#<GitHub Issue番号>` | 起票された開発要求 |
| criterion | `AC-<n>` | 受け入れ条件 |
| gate | `G-<n>` | 方式そのものの継続判断 |

requirement だけは GitHub Issue 番号を ID とする。lak は Issue 番号を**識別子として持つだけ**で、GitHub API は叩かない（§4.6）。

### 4.2 エッジ

| ラベル | 向き | 意味 |
|---|---|---|
| `closes` | question → decision | 論点を閉じた決定 |
| `narrows` | decision → decision | 成立範囲を狭める |
| `widens` | decision → decision | 成立範囲を広げる |
| `supersedes` | decision → decision | 置き換える |
| `completes` | decision → decision | 未決だった部分を補う |
| `targets` | need → criterion | ニーズが担う受け入れ条件 |
| `spawned-by` | need → decision | その決定によって生じたニーズ |
| `filed-as` | need → requirement | 起票されて要求になった |
| `depends-on` | need → need | 着手前提 |
| `relies-on` | requirement → decision | 依拠する決定 |
| `raised` | requirement → question | 新たに生じた論点 |
| `measured-by` | gate → question | ゲートの判断材料 |

**エッジは両側の frontmatter に書き込む。** 片側だけを書いて他方を導出しない。`lak show` が片側だけを読んで完結できることを優先する。整合は `lint` が見る。

> **実装メモ**: `widens` は実運用で使われた実績がない。実装はするが、使用実績が0のまま3プロジェクトを終えたら削除を検討する。

### 4.3 ID の規則

- **発番は `lak` のみがおこなう。** 手で番号を書かない
- **ID は不変である。** 採番後に変更・再利用・詰め直しをしない
- **削除しない。** 論点が閉じても行は残り、内容が「→ `D-xx` で決定済み」に置き換わる。IDが宙に浮くと過去の Issue からの参照が切れる
- 発番はスコープ横断で一意とする（§5.2）

### 4.4 ファイル形式

**1ノード1ファイル。YAML frontmatter + Markdown 本文。frontmatter が正本である。**

```markdown
---
id: D-147
type: decision
title: 配信順序を発行時刻の昇順に固定する
scope: new_payment_gateway_migration_v2
created: 2026-09-08
decision_scope: |
  本番の配信ワーカーのうち、優先度キューを使う経路にのみ適用する。
  バッチ再送経路は対象外。
closes: [Q-73]
narrows:
  - id: D-138
    mark: "並び順は実装依存としてよい（3節2文目）"
---

## Context

## Decision

## Consequences
```

**frontmatter の属性集合は開いている。** 未知のキーを読み捨てず、保存し、検索可能にすること（§7.2）。必要な属性はドッグフーディングで見えてくるため、スキーマを固定しない。

必須キーは `id` / `type` / `title` / `scope` / `created` の5つのみ。それ以外は型ごとの推奨キーとし、欠落は `lint` の検査項目として扱う（エラーにするか警告にするかは `lak.toml` で設定可能）。

### 4.5 ディレクトリ構成

```
docs/ledger/
  <scope>/
    decisions/D-147.md
    questions/Q-73.md
    needs/N-12.md
    requirements/6006.md
    criteria/AC-3.md
    gates/G-1.md
  lak.toml
```

`render` の出力先は `lak.toml` で指定する。既定は `docs/ledger/<scope>/README.md`。

### 4.6 GitHub との関係

**lak は GitHub API を叩かない。** requirement ノードは Issue 番号・PR URL・状態を frontmatter に持つが、それらの実在確認はしない。

理由は2つ。ネットワークなしで `lint` が回ること。そして**台帳と実態の照合は人間とエージェントの仕事**であり、機械化すると「lint が通った＝実態と合っている」という誤った安心を生むため（§11.2）。

ただし `req advance` のガードで PR の base・ファイル数を検査する項目がある（§6.6）。これは**報告された値と frontmatter の値の照合**であり、GitHub への問い合わせではない。

---

## 5. スコープ

### 5.1 定義

スコープはプロジェクト識別子である（例: `new_payment_gateway_migration_v2`）。台帳のディレクトリを分ける単位。

### 5.2 横断が既定

**すべての読み取り系コマンド（`find` / `show` / `lint` / `next` / `stats`）は、全スコープを横断するのが既定である。** `--scope <name>` で絞り込める。

理由：20領域の基幹システムで複数プロジェクトを並行させており、**決定はプロジェクトをまたいで効く**。プロジェクトAで決めたことがプロジェクトBの設計の前提になる。既定が単一スコープだと、Bの担当はAの決定を引けない。

書き込み系コマンド（`need add` / `question add` / `decide` など）は、カレントディレクトリから解決したスコープに書く。解決できない場合はエラーとし、`--scope` を要求する。

ID の発番は全スコープで一意とする。`D-147` はプロジェクトをまたいで1つしか存在しない。

> **未定**: 横断すべきでない決定（プロジェクト固有で他に漏らしたくないもの）の扱い。ドッグフーディングで必要が見えたら `visibility: local` のような属性を足す。初版では実装しない。

---

## 6. コマンド仕様

すべてのコマンドは §8 の終了コード規約に従う。すべての書き込み系コマンドは `--json` で機械可読な結果を返せること（エージェントが主利用者のため）。

### 6.1 `lak init <scope>`

台帳を作る。`docs/ledger/<scope>/` と `lak.toml` を作り、台帳の場所を指す1行を `AGENTS.md` に追記する。

既存の `AGENTS.md` がある場合は追記のみ。上書きしない。

### 6.2 `lak need`

```
lak need add "<ニーズの内容>" --targets AC-3 [--spawned-by D-101]
lak need file N-12 --issue 6006
```

- **`--targets` は必須。** どの受け入れ条件も担わないニーズは、進捗の俯瞰に乗らないため作れない。これは実際に2件が欄から抜けた失敗への対処である
- `--targets` に存在しない `AC-x` を指定したらエラー
- `need file` は `filed-as` を張り、状態を「設計待ち」へ進める
- **`--issue` が親Issueのサブとして紐づいていることを frontmatter 上で確認できない場合は拒否する**（親Issue番号は `lak.toml` に持つ）

### 6.3 `lak question`

```
lak question add "<論点>" --decider master --options "<案A>" --options "<案B>" [--bundle <束の名前>] [--force]
lak question close Q-64 --by fact --note "<なぜ事実で消えたか>"
lak question close Q-73 --by decision --decision D-147
lak question close Q-01 --by non-decision --decision D-114
```

**`add` は2つの検査を通る。**

1. **横断検索を自動で回す。** 既存の決定・台帳のヒットを表示し、「この論点は既に答えられていないか」を確認させる。ヒットがある場合、`--force` なしでは作らない
2. **`--options` を2つ以上要求する。** 選択肢を2つ以上書けない論点は作れない。「選択肢が実在せず、事実で一意に決まるもの」を論点として立てさせないため

`--decider` は必須。人間単独で決められるものと社外・他部署との合意が要るものはリードタイムが違い、区別しないと着手順を誤る。

`--bundle` を付けると論点キューの束に入る。**束には「同じ手当てで閉じるか」の記述を必須とする**（`--bundle-rationale`）。束の判定は「原因が同じか」ではなく「同じ手当てで閉じるか」でおこなう。

**`close` の閉じ方は3つだけ。** `fact` / `decision` / `non-decision`。

- `--by decision` と `--by non-decision` は `--decision` を必須とする。無ければ `lak decide` を促すメッセージを出して終了コード2で終わる
- `--by fact` は `--note` を必須とする
- **閉じた瞬間、その論点を「未決」として参照している全ノードを列挙して警告する。** AC-2 が Q-52 を「決着待ち」と書いたまま残った失敗を、閉じた時点で検出する

#### `lak q "<一文>"` （短縮入力）

人間が作業中に論点を思いついた瞬間のための入口。`--options` も `--decider` も要求せず、不完全なノードを作る。**作られたノードは `lak lint` が必ず落とす。**

理由：関門が3つあると、人間は台帳の外にメモする。8割しか埋まっていない台帳は、欠落が見えないぶん、無い台帳より危険である。「作らせない」のではなく「作らせて検出する」。

エージェントはこのコマンドを使わないこと（`--help` に明記する）。

### 6.4 `lak decide`

```
lak decide "<決定を1文で言い切る>" --closes Q-73 --scope-note "<成立範囲>"
```

- **成立範囲は必須引数とする。** 「成立範囲を省略しない」を規則ではなく必須項目にする。ここが崩れると、後続の担当が決定を広く読んで誤る
- `--closes` を指定すると `question close --by decision` を自動で回す
- **作成時に、開いている論点の一覧を提示し「この決定が答えているものはないか」を問う。** 決定が偶然答えている論点は、`--closes` を書かない限り開いたまま残るため

`--scope` （台帳スコープ）と `--scope-note` （決定の成立範囲）は名前が紛らわしい。**引数名は受注側でより良いものに変えてよい**が、2つが別物であることが `--help` から明確であること。

### 6.5 `lak link`

```
lak link D-148 supersedes D-39 --mark "<古い側のどの記述が失効したか>"
lak link D-147 narrows D-138 --mark "..."
```

- 関連は**両側の frontmatter へ張る**（adrs の逆向きリンク自動導出と同じ発想）
- `--mark` を付けると、古い決定の当該記述の位置に印を挿す。**本文は書き換えず、印だけを足す**
- **`narrows` / `supersedes` で `--mark` を省略すると警告する**

`--mark` の目的は「古い決定だけを読んだ人が誤らないこと」である。ファイル単位のリンクだけでは、Context のどの文が死んだかを指せない。

> **実装上の注意**: 本文への印の挿入は、本文が後から編集されると位置が壊れる。印はアンカーとなる文字列そのものを frontmatter に持ち、本文への挿入は `render` / `show` の表示時におこなう方式でもよい。**位置が壊れないことを優先すること。**

### 6.6 `lak req`

```
lak req advance 6006 --to マージ待ち --evidence https://github.com/.../pull/6007
lak req compress 6001
```

#### `advance`

**状態は次の11語に固定し、`--to` はここからしか選べない。** 括弧で文脈を添えることは許す（`設計待ち (振り出し済み)` / `実装待ち (後始末)` / `実装待ち (第2段)`）。状態集合そのものは増やさない。

```
未起票 / 要求化中 / 設計待ち / 承認待ち / 実装待ち / 監査待ち /
PR待ち / マージ待ち / 本番作業待ち / 後始末待ち / 完了
```

遷移表のガードを検査する。主要なもの：

- **マージ待ちへ進めるには**、PR の URL が存在し、base とファイル数が報告と一致すること
- **完了へ進めるには**、残作業が0であること
- **本番作業待ち / 後始末待ち / 完了 の判定**は、データ移行の有無と「本番でしか確かめられない変更か」を問い、答えから決める。移行が無いことは後者に当たらないことを意味しない

**`--evidence` は必須。** 状態を抜けるときの根拠が常に残る。

> **未定**: 遷移表の全域（全遷移とガード）を初版で焼き込むか。**推奨は、状態集合と `--evidence` 必須と上記3つのガードだけを初版に入れ、遷移表全体は `lak.toml` の設定として外に出すこと。** 遷移表はプロジェクト固有性が最も高く、ハーネス自体がまだ改訂される部分である。

#### `compress`

完了した要求の記録を圧縮する。残すのは6項目のみ。

- **圧縮の前に「後続が引く制約」を1件ずつ確認し、決定に立っていないものを列挙する。** 立っていなければ `decide` を促し、作らない限り圧縮を拒否する
- 圧縮前の全文は、該当 Issue のコメントへ退避する前提で**標準出力に出す**（lak は API を叩かないため、退避は呼び出し側がおこなう）。退避先の URL を `--evidence` で受け取り、frontmatter に残す

これは、注文明細の重複防止が1つの Issue の行にだけ残っていた失敗への対処である。

### 6.7 `lak lint`

グラフ全体の整合性を検査する。**終了コードで失敗を伝える。CI と pre-commit で回す。**

検出項目：

| # | 検出するもの |
|---|---|
| L1 | 閉じた論点を未決として参照しているノード |
| L2 | 受け入れ条件が「担い手 N 件」と書く数と、`targets` で繋がるニーズの数の不一致 |
| L3 | どの受け入れ条件も担わないニーズ |
| L4 | 帰属が2箇所以上にある論点 |
| L5 | 置き換えられた決定に依拠している要求 |
| L6 | `narrows` / `supersedes` で印のない決定 |
| L7 | 成立範囲が空の決定 |
| L8 | 決定者が空の論点 |
| L9 | 選択肢が1つ以下の論点 |
| L10 | 状態が11語の外にある要求 |
| L11 | 完了と記録されているのに残作業を持つ要求 |
| L12 | 束に入っているのに「同じ手当てで閉じるか」の記述がない論点 |
| L13 | 参照先が存在しないID（宙に浮いた参照） |

**各項目は `lak.toml` の `[lint]` で個別に on/off と重大度（error / warn）を設定できること**（adrs の `[doctor]` と同じ）。ドッグフーディングで鳴らない項目を黙らせるため。

出力は `--json` でも返せること。

### 6.8 `lak render [--format markdown|dot]`

台帳を生成する。**手で表を書かないので書式が崩れない。** 列の構成・IDの並び・閉じた論点の書式は render が決める。

**出力を1ファイルに固定しない。ノード数に応じて分割する。** GitHub の本文上限に当たって緊急の圧縮をした失敗への対処。分割の閾値は `lak.toml` で設定可能とする。

### 6.9 `lak show <ID> [--graph]`

ノードとその周辺を表示する。「この決定は何を覆したか、何に依拠されているか、どのニーズを生んだか」が1回で読めること。

`--graph` で関連を辿る。Graphviz 出力を持たせてよい。

### 6.10 `lak find <キーワード>`

決定・論点・ニーズ・受け入れ条件を横断する。**全スコープが既定。**

- ヒットした節を示す
- **Context や成立範囲のヒットも決定の一部として扱う。** Decision 節に無いことを「記述が無い」と読ませない
- frontmatter の属性で絞り込めること（§7.2）

### 6.11 `lak next`

次に着手できるものを列挙する。`depends-on` が解けていて、閉じていない論点に依拠していないニーズだけを出す。

**優先順位は付けない。並べるだけで、選ぶのは人間である。**

### 6.12 `lak handover`

引き継ぎの検査。台帳から状態を復元できるかを機械で確かめる。

- すべての進行中の要求に、次の遷移の根拠と責任主体が対応しているか
- 宙に浮いたIDがないか
- `lint` が通るか

**出力は事実の列挙に留めること。「引き継ぎ可能」という判定語を返さない。**

理由は §11.2 に述べる。検査しているのはグラフの内部整合であって、台帳が実態と合っているかではない。判定語を返すと、緑であることが実態との一致を意味すると読まれる。

出力例：
```
lint: 通過（13項目）
進行中の要求: 4件。うち次遷移の根拠と責任主体が揃っているもの 4件
宙に浮いたID: なし
※ これはグラフの内部整合の検査です。台帳と実態の一致は検査していません。
```

### 6.13 `lak stats`

進捗を二軸で出す。

- **主軸**: 受け入れ条件の充足度（単調に増える）
- **副軸**: 論点の**新規発生率の減衰**（残数ではない）

不確実性の高い初期では論点の残数は必ず増えるため、残数を進捗指標にすると誤読する。

台帳が git 上のファイルであることを利用し、**新規発生率は git 履歴から算出する**。人間が数える必要のある指標を人間に数えさせない。

### 6.14 `lak cheatsheet`

主要ワークフローの早見表を出す。エージェントのセッション開始時に読ませる用途。

---

## 7. frontmatter と検索

### 7.1 拡張可能であること（必須）

**frontmatter の未知のキーを読み捨てないこと。** 保存し、`render` を通しても失われず、検索の対象になること。

必要な属性は運用しながら見えてくる。スキーマを固定すると、属性を足すたびに実装変更が要る。

### 7.2 属性による検索（必須）

未知のものを含む任意の frontmatter 属性で絞り込めること。

```
lak find <キーワード> --where decider=master
lak find --where type=decision --where scope=new_payment_gateway_migration_v2
lak find --where "created>=2026-09-01"
```

構文は受注側の裁量とする。**要求は「frontmatter に書いた属性は、実装を変更せずに検索で使える」こと。**

---

## 8. 終了コードと出力

| コード | 意味 |
|---|---|
| 0 | 成功 |
| 1 | 検査の失敗（`lint` が error 重大度の項目を検出した） |
| 2 | 入力の不備（必須引数の欠落、存在しないIDの参照、ガード違反） |
| 3 | 台帳の不整合で処理を続行できない（パース失敗など） |

- 標準出力は結果、標準エラーは診断に分ける
- すべてのコマンドに `--json` を用意する（エージェントが主利用者のため）
- `--quiet` / `--verbose` を用意する

---

## 9. エラーメッセージと `--help`（重要）

**運用文書から消える9項目の実際の置き場所は、`--help` とエラーメッセージである。**

必須引数を省略したときのエラーは、**なぜそれが必須なのかを1行で説明すること。**

悪い例：
```
error: the following required arguments were not provided: --scope-note <SCOPE_NOTE>
```

良い例：
```
error: --scope-note（成立範囲）は省略できません

  成立範囲を書かない決定は、後続の担当が広く読んで誤ります。
  この決定がどの経路・どの条件でのみ成り立つかを書いてください。

  例: lak decide "..." --scope-note "優先度キューを使う配信経路にのみ適用。バッチ再送は対象外"
```

同様に説明を要するもの：

| 対象 | 説明すべきこと |
|---|---|
| `need add --targets` | どの受け入れ条件も担わないニーズは進捗の俯瞰から消える |
| `question add --options` | 選択肢が実在せず事実で一意に決まるものは論点ではない |
| `question close --by` | 閉じ方は3つだけであること、決定になるのは2つ |
| `link --mark` | ファイル単位のリンクではどの文が死んだか指せない |
| `req advance --evidence` | 状態を抜けるときの根拠を常に残すため |
| `req compress` の拒否 | 後続が引く制約が決定に立っていない |

これらは受注側が文言を起草し、発注者がドッグフーディングで改訂する。**文言が空疎な機械翻訳的説明にならないこと。**

---

## 10. エージェント連携

### 10.1 MCP サーバ（必須）

adrs と同様、MCP サーバを内蔵する。`lak mcp serve` で起動し、主要コマンドをツールとして公開する。

利用者の大半がAIエージェントであるため、この機能の重要度は adrs における重要度より高い。

### 10.2 組み込み skills（必須）

`lak` のバイナリに、エージェント向けの利用手順を同梱する。`lak skills install <dir>` のような形で、リポジトリの skills ディレクトリへ展開できること。

同梱する内容（初版）：

| skill | 内容 |
|---|---|
| `lak-ledger` | 台帳の読み方、ノードとエッジの意味、セッション復旧の手順 |
| `lak-question` | 論点を立てる前の検査、閉じ方3通りの使い分け |
| `lak-decide` | 成立範囲の書き方、リンクと `--mark` の使い分け |

**規律がツールと一緒に運ばれることが、このツールの狙いの半分である。**

---

## 11. 設計上の注意（発注者から受注者へ）

### 11.1 書き手はエージェントである

`--options` を2つ以上要求するガードは、人間に対しては「捏造が面倒」という形で効く。**LLM に対しては効かない。** もっともらしい第2案を無限に生成できる。`--scope-note` も必ず埋まるが、中身が空疎でも `lint` は通る。

**この問題を lak で解こうとしないこと。** 解けない。運用文書側に「機械で測れない不変条件」として残す。

受注側への要求は1つだけ：**`lint` が緑であることが何を意味しないかを、出力の中で明示すること**（§6.12）。

### 11.2 検査しているのは整合性であって正しさではない

`AC-3` を実際には担っていないニーズに `--targets AC-3` と書いても lint は通る。これは避けようがない。

避けられないからこそ、**出力の言葉を強くしないこと。** 「引き継ぎ可能」「検証済み」「担保されている」といった語を使わない。事実を並べる。

### 11.3 台帳の編集権限

現在の運用では、台帳を編集するのは進行管理担当1人だけと定めている。これは (a) ID発番を1箇所に集めるため (b) 台帳の整合を1人が守るため、の2つが理由である。

**`lak` が完成すると、(a) は lak の発番が、(b) は `lint` が解決する。** したがって編集権限の集約は不要になりうる。

受注側への要求：**特定の主体だけが書けるという制約を実装に埋め込まないこと。** 誰が実行しても同じように動くこと。権限の運用は発注者側の判断で変える。

### 11.4 既存資産

現在、決定は `docs/adr/` に adr-tools 互換の形式で存在し、台帳は GitHub Issue 本文と Markdown ファイルに分散している。

**移行コマンドを用意すること。** `lak import` で既存の ADR ディレクトリを decision ノードとして取り込めること。**ID は保存する**（`D-147` は `D-147` のまま）。Issue 本文からの取り込みは手作業でよい。

---

## 12. 受け入れ条件

| # | 条件 |
|---|---|
| A1 | §6 の全コマンドが動作する |
| A2 | §6.7 の L1〜L13 を検出し、終了コードで伝える |
| A3 | `lak.toml` の `[lint]` で各項目の on/off と重大度を設定できる |
| A4 | 全読み取りコマンドが全スコープ横断で既定動作する |
| A5 | frontmatter に未知のキーを書いても失われず、`find --where` で検索できる |
| A6 | サブディレクトリから実行しても台帳を発見できる |
| A7 | 全コマンドが `--json` を持つ |
| A8 | 必須引数の欠落時のエラーが、§9 の水準で理由を説明する |
| A9 | MCP サーバが起動し、主要コマンドをツールとして公開する |
| A10 | 組み込み skills をリポジトリへ展開できる |
| A11 | `lak import` で既存 ADR を ID を保存したまま取り込める |
| A12 | `cargo install lak` でインストールできる |
| A13 | `.pre-commit-hooks.yaml` を同梱し、pre-commit で `lak lint` が回る |
| A14 | `handover` の出力が判定語を含まず、事実の列挙に留まる |

---

## 13. 意図的に決めていないこと

以下はドッグフーディングで決める。**初版では実装せず、拡張できる形だけ残すこと。**

| 項目 | 現状の扱い |
|---|---|
| 遷移表の全域 | 状態集合と主要3ガードのみ実装。全域は `lak.toml` へ外出しを推奨 |
| スコープ横断の例外（local な決定） | 実装しない。属性で後から足せる形にする |
| `widens` エッジの存否 | 実装する。使用実績が出なければ後で削る |
| lint 項目の追加 | `[lint]` の設定機構があれば足せる |
| `render` の列構成 | 実装者が決める。ドッグフーディングで改訂する |
| `find --where` の構文 | 実装者が決める |
| Web UI | 作らない |

---

## 付録: 運用文書から消える項目

`lak` の完成により、エージェントハーネス（現在約1300行）から以下が消える。**受注側は、これらが lak のどこで代替されるかを確認すること。**

| 消えるもの | lak での置き場所 |
|---|---|
| 状態集合の11語と遷移表 | `req advance` のスキーマとガード |
| 不変条件 I1〜I12 のうち9件 | `lint` の検査項目 |
| 論点の閉じ方3通り | `question close --by` |
| 決定の成立範囲を省略しない | `decide` の必須引数 |
| リンクのラベル6種と判定基準 | `link` のサブコマンド |
| 圧縮の6項目と落とすもの | `req compress` |
| 台帳の表の列構成 | `render` |
| adr-find を通す手順 | `question add` が自動で回す |
| ニーズが俯瞰から消える注意 | `need add --targets` 必須 |

**残るもの**（機械が判定できないため文書が持つ）：

- 目的と問題、役割の定義、検査の分担
- I2: 設計の承認者は常に人間である
- I10: 兼任時は、どの発言が仮説でどこで人間が決定したかがログ上で区別できる
- I11: コミット・push・本番操作は、明示的に振り出された範囲だけ実行されている
- **（新規）選択肢や成立範囲が形式的に埋まっているだけでないかは、人間が見る**
- 振り出しと報告の様式
- 人間とのコミュニケーション（台帳を読む手順を人間は持たない、という非対称）
